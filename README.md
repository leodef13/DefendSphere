# 🛡️ DefendSphere - Security Dashboard

**DefendSphere** - это комплексная платформа для мониторинга безопасности, управления инцидентами и обеспечения соответствия требованиям. Система предоставляет современный веб-интерфейс с расширенными возможностями администрирования и мультиязычной поддержкой.

## ✨ Основные возможности

### 🔐 Аутентификация и авторизация
- JWT-based аутентификация
- Ролевая модель доступа (Admin/User)
- Защищенные маршруты
- Управление сессиями

### 🌐 Мультиязычность
- Поддержка русского, английского и испанского языков
- Динамическое переключение языков
- Локализованный интерфейс

### 👥 Управление пользователями
- Регистрация и вход пользователей
- Административная панель
- Управление ролями и правами доступа
- Профили пользователей

### 📊 Основные модули
- **Home** - Главная страница с обзором системы
- **Starter Guide** - Руководство по началу работы
- **Reports** - Генерация и просмотр отчетов
- **Compliance** - Мониторинг соответствия стандартам
- **Customer Trust** - Индикаторы доверия клиентов
- **Suppliers** - Управление поставщиками
- **Assets** - Мониторинг активов
- **Integrations** - Внешние интеграции

### 🛠️ Административная панель
- Управление пользователями (CRUD)
- Настройка ролей и разрешений
- Системная статистика
- Мониторинг активности

## 🏗️ Архитектура

### Frontend
- **React 18** с TypeScript
- **Vite** для сборки
- **React Router** для навигации
- **TailwindCSS** для стилизации
- **Framer Motion** для анимаций
- **Recharts** для графиков
- **Lucide React** для иконок

### Backend
- **Node.js** с Express.js
- **Redis** как основная база данных
- **JWT** для аутентификации
- **bcryptjs** для хеширования паролей
- **CORS** и **Morgan** для middleware

### База данных
- **Redis** для хранения пользователей, сессий и метрик
- Автоматическая инициализация с тестовыми данными
- Поддержка кластеризации

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+
- npm или yarn
- Redis (локально или в Docker)
- Docker и Docker Compose (опционально)

### 1. Клонирование репозитория
```bash
git clone https://github.com/leodef13/DefendSphere.git
cd DefendSphere
git checkout cursor/defendsphere-343d
```

### 2. Автоматический запуск (рекомендуется)
```bash
# Сделать скрипт исполняемым
chmod +x scripts/quick-start.sh

# Запустить проект
./scripts/quick-start.sh
```

### 3. Ручной запуск
```bash
# Установка зависимостей
make install

# Инициализация Redis
make redis-init

# Запуск в режиме разработки
make start
```

### 4. Запуск через Docker
```bash
# Сборка и запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```
# Сборка и запуск
make docker-build
make docker-up

# Проверка статуса
make status
```

## 🔑 Тестовые пользователи

После инициализации системы доступны следующие пользователи:

| Пользователь | Пароль | Роль | Доступ |
|--------------|--------|------|--------|
| `admin` | `admin` | Admin | Полный доступ ко всем модулям |
| `user1` | `user1` | User | Ограниченный доступ |
| `user2` | `user2` | User | Ограниченный доступ |

## 📱 Доступные URL

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5000
- **Admin Panel**: http://localhost:3000/admin
- **API Documentation**: http://localhost:5000/api

## 🛠️ Команды управления

### Основные команды
```bash
make help          # Показать справку
make install       # Установить зависимости
make build         # Собрать проект
make start         # Запустить в режиме разработки
make stop          # Остановить все процессы
make clean         # Очистить build файлы
```

### Docker команды
```bash
make docker-build  # Собрать Docker образы
make docker-up     # Запустить в Docker
make docker-down   # Остановить Docker контейнеры
make docker-restart # Перезапустить контейнеры
```

### Мониторинг
```bash
make status        # Статус всех сервисов
make logs          # Логи контейнеров
make redis-cli     # Подключение к Redis CLI
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Информация о текущем пользователе

### Пользователи
- `GET /api/admin/users` - Список пользователей (Admin)
- `POST /api/admin/users` - Создание пользователя (Admin)
- `PUT /api/admin/users/:id` - Обновление пользователя (Admin)
- `DELETE /api/admin/users/:id` - Удаление пользователя (Admin)
- `PUT /api/users/profile` - Обновление профиля

### Система
- `GET /api/health` - Проверка здоровья системы

## 🌍 Мультиязычность

Система поддерживает три языка:
- **English** (en) - по умолчанию
- **Русский** (ru)
- **Español** (es)

Переключение языков доступно в правом верхнем углу интерфейса.

## 🔒 Безопасность

### Аутентификация
- JWT токены с настраиваемым временем жизни
- Хеширование паролей с bcrypt
- Защита от CSRF атак

### Авторизация
- Ролевая модель доступа
- Проверка разрешений на уровне маршрутов
- Middleware для защиты API endpoints

### Защита данных
- Валидация входных данных
- Санитизация пользовательского ввода
- Логирование действий пользователей

## 📊 Мониторинг и логирование

### Системные метрики
- Количество активных пользователей
- Время отклика API
- Использование ресурсов
- Статус внешних интеграций

### Логирование
- Логи аутентификации
- Административные действия
- Ошибки системы
- Аудит доступа

## 🐳 Docker развертывание

### Структура контейнеров
- **redis**: База данных Redis
- **backend**: Node.js API сервер
- **frontend**: React приложение с Nginx

### Переменные окружения
Скопируйте `.env.example` в `.env` и настройте:
```bash
cp .env.example .env
# Отредактируйте .env файл
```

### Запуск продакшена
```bash
# Сборка и запуск
docker-compose -f docker-compose.yml up -d

# Проверка статуса
docker-compose ps
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Frontend тесты
npm test

# Backend тесты
cd backend && npm test

# Все тесты
make test
```

### Тестовые данные
Система автоматически создает тестовые данные при инициализации:
- Пользователи с разными ролями
- Примеры инцидентов и алертов
- Системные метрики
- Настройки интеграций

## 📚 Документация

- [ADMIN_README.md](./ADMIN_README.md) - Документация по административной панели
- [API Documentation](./docs/api.md) - Детальное описание API
- [Deployment Guide](./docs/deployment.md) - Руководство по развертыванию

## 🤝 Разработка

### Структура проекта
```
DefendSphere/
├── src/                    # Frontend исходный код
│   ├── components/        # React компоненты
│   ├── contexts/          # React контексты
│   ├── layouts/           # Макеты страниц
│   ├── pages/             # Страницы приложения
│   └── admin/             # Административные страницы
├── backend/               # Backend API
├── scripts/               # Скрипты инициализации
├── docker-compose.yml     # Docker конфигурация
├── Makefile               # Команды управления
└── README.md              # Документация
```

### Добавление новых модулей
1. Создайте страницу в `src/pages/`
2. Добавьте маршрут в `src/App.tsx`
3. Обновите навигацию в `src/layouts/MainLayout.tsx`
4. Добавьте переводы в `src/contexts/LanguageContext.tsx`

### Добавление новых ролей
1. Обновите типы в `src/types/auth.ts`
2. Добавьте разрешения в `src/contexts/AuthContext.tsx`
3. Обновите backend middleware
4. Добавьте UI элементы в админ панели

## 🚨 Устранение неполадок

### Частые проблемы

#### Redis не запускается
```bash
# Проверка статуса Redis
redis-cli ping

# Запуск Redis
redis-server --port 6379
```

#### Backend не отвечает
```bash
# Проверка логов
tail -f backend.log

# Проверка порта
netstat -tlnp | grep 5000
```

#### Frontend не загружается
```bash
# Проверка сборки
npm run build

# Проверка порта
netstat -tlnp | grep 3000
```

### Логи и отладка
```bash
# Просмотр логов
make logs

# Статус сервисов
make status

# Перезапуск
make docker-restart
```

## 📈 Производительность

### Оптимизации
- Gzip сжатие для статических файлов
- Кэширование Redis
- Lazy loading компонентов
- Оптимизация изображений

### Масштабирование
- Горизонтальное масштабирование backend
- Redis кластеризация
- Load balancing для frontend
- Мониторинг ресурсов

## 🔄 Обновления

### Обновление системы
```bash
# Получение последних изменений
git pull origin cursor/defendsphere-343d

# Обновление зависимостей
make install

# Перезапуск
make docker-restart
```

### Миграции данных
- Автоматическое обновление схемы Redis
- Совместимость версий
- Резервное копирование данных

## 📞 Поддержка

### Сообщество
- GitHub Issues для багов
- GitHub Discussions для вопросов
- Pull Requests приветствуются

### Контакты
- **Автор**: leodef13
- **Репозиторий**: https://github.com/leodef13/DefendSphere
- **Ветка**: cursor/defendsphere-343d

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](./LICENSE) для получения дополнительной информации.

---

**DefendSphere** - защита вашей цифровой инфраструктуры начинается здесь! 🚀

